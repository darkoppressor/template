#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR

cd ../development/android/

./android-prep-linux-x86_64 > ${DIR}/log-Release-Android

cd build-scripts/linux/

./build >> ${DIR}/log-Release-Android

./ant-release-build >> ${DIR}/log-Release-Android
