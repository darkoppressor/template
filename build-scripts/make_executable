#!/bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd $DIR

chmod +x *
chmod -x version

chmod +x ../development/android/adb-logcat
chmod +x ../development/android/adb-logcat-all
chmod +x ../development/android/android-prep-linux-x86_64

chmod +x ../development/android/build-scripts/linux/*

chmod -x ../development/android/build-scripts/linux/ant.properties
chmod -x ../development/android/build-scripts/linux/local.properties
